# list of sub-projects for current project
gradle projects

# sub-project tasks
gradle :sub-project-1:tasks

# generate Gradle wrapper
gradle wrapper
# ./gradle/wrapper/* files, as well as ./gradlew, ./gradlew.bat should be under version control,
# as that allows to build a project with ./gradlew even if Gradle has never been installed on the system.
